## Copilot-Specific Notes
- Generated from this repo's `prompts/core.md` plus `prompts/harnesses/copilot.md`. Edit the source fragments, then run `scripts/sync-ai-prompts`.
- Target path is `~/.copilot/AGENTS.md` by default. Override with `COPILOT_AGENTS_PATH` if your install uses a different global rules path.
- For scripted work, verify current GitHub Copilot CLI non-interactive flags before use.
- Keep prompts explicit about repository context, desired files, and verification because Copilot behavior differs across IDE and CLI surfaces.
