## Goose-Specific Notes
- Generated from this repo's `prompts/core.md` plus `prompts/harnesses/goose.md`. Edit the source fragments, then run `scripts/sync-ai-prompts`.
- Target path is `~/.config/goose/AGENTS.md` by default. Override with `GOOSE_AGENTS_PATH` if your setup differs.
- Verify current Goose extension, provider, and CLI config before relying on automation.
