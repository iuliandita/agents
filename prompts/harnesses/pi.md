# Pi Coding Agent

- Load `AGENTS.md` context hierarchically; distinguish project instructions from provider credentials and local runtime config.
- Respect `--no-context-files` or equivalent user flags when present and state when global rules were intentionally bypassed.
