# Crush

- Treat `CRUSH.md` as operational coding-agent guidance, with shared `AGENTS.md` context only when the user or project provides it.
- Keep file edits small and verify command output because Crush workflows often run inside terminal-first agent loops.
