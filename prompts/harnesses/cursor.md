## Cursor-Specific Notes
- Generated from this repo's `prompts/core.md` plus `prompts/harnesses/cursor.md`. Edit the source fragments, then run `scripts/sync-ai-prompts`.
- Target path is `~/.cursor/AGENTS.md` by default. Override with `CURSOR_AGENTS_PATH` if your install uses a different global rules path.
- Keep repo-local Cursor rules aligned with `AGENTS.md` and `CLAUDE.md` when a project tracks both.
