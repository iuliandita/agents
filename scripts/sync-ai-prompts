#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

source "$repo_root/scripts/python-runtime.sh"
select_agents_python "$repo_root"

exec "$agents_python" "$repo_root/scripts/render_prompts.py" --repo-root "$repo_root" "$@"
